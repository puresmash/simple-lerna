{
  "private": "true",
  "devDependencies": {
    "lerna": "^2.8.0"
  },
  "name": "simple-lerna",
  "description": "```shell npm i -g lerna git init simple-lerna cd simple-lerna lerna init ```",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "swat",
  "license": "ISC"
}
